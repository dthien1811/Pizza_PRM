Quản lý Tài khoản (Client-side)
1. Đăng ký (UI, validation, call API). 2. Đăng nhập/Đăng xuất (UI, lưu trữ Token/Session). 3. Màn hình thông tin cá nhân (Xem, Cập nhật thông tin).
Tập trung vào bảo mật client-side và UX đăng nhập.

Gian hàng (Xem & Chi tiết)
1. Xem Danh sách Pizza (Recycler View, Load data từ API). 2. Tìm kiếm/Lọc Pizza (Logic tìm kiếm, hiển thị kết quả). 3. Xem Chi tiết Pizza (Màn hình chi tiết, hiển thị thuộc tính).
Tập trung vào hiệu năng hiền thị danh sách lớn và logic tìm kiếm.


Giỏ hàng (Tương tác)
1. Thêm/Bớt sản phẩm vào Giỏ hàng (Logic Giỏ hàng local/Database, cập nhật số lượng).
Tập trung vào logic Giỏ hàng phức tạp (tính toán, lưu trữ trạng thái).
2. Xem Giỏ hàng (UI Giỏ hàng, tính tổng tiền, hiển thị chi tiết).
3. Màn hình Đặt hàng (Thu thập địa chỉ, SĐT, ghi chú).

Thanh toán (Logic)
1. Chọn phương thức Thanh toán (UI lựa chọn). 2. Thực hiện Thanh toán (Logic) (Xử lý thông tin thanh toán, tích hợp API thanh toán/logic cơ bản cho COD). 3. Quản lý API (Thiết kế cấu trúc API liên quan đến order, payment).
Tập trung vào phân logic giao dịch và API, có thể dùng Mock ΑΡΙ.


Quản lý Đơn hàng (Theo dõi)
1. Xác nhận Đơn hàng (Gửi order lên hệ thống, nhận mã đơn hàng). 2. Xem Lịch sử Đơn hàng (Load danh sách đơn hàng đã đặt). 3. Theo dõi Trạng thái đơn hàng (Hiển thị trạng thái cập nhật). 4. Giao diện Admin cơ bản (Ví du: xem
Tập trung vào việc đồng bộ hóa dữ liệu đơn hàng và hiển thị trạng thái động. Hien thị trạng thái động.
đơn hàng). 
2. Xem Lịch sử Đơn hàng (Load danh sách đơn hàng đã đặt). 3. Theo dõi Trạng thái đơn hàng (Hiển thị trạng thái cập nhật). 4. Giao diện Admin cơ bản (Ví dụ: xem danh sách đơn hàng).



